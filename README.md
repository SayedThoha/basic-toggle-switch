# Angular Toggle Checkbox Example using BehaviorSubject

This Angular application demonstrates how to **toggle a checkbox** and **share its state across components** using a shared service and `BehaviorSubject`.